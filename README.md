# rserve

A pure rust implementation of the npm `serve` package.

## Quickstart Usage

```bash
# with pnpm
pnpm install -g rserve

# with npm
npm i -g rserve

# with yarn
yarn add --global rserve
```

```bash

```
